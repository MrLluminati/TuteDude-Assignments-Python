# Assignment 7: Module 16 - Building Database Apps with PostgreSQL & Python

Source PDF:

```text
../../resources/source_pdfs/ASSIGNMENT 7.docx.pdf
```

Compatible module note:

```text
../../resources/module_notes/Module 12 Building Database Apps with PostgreSQL & Python.pdf
```

## Status

Implemented. Local PostgreSQL setup and screenshot evidence are still required before final portal submission.

## Project Scope

This assignment demonstrates how to connect Python with PostgreSQL and perform database operations using `psycopg2`.

Implemented practical files:

| File | Purpose |
| --- | --- |
| `.env.example` | Template for local PostgreSQL credentials. |
| `requirements.txt` | Python dependencies for the project. |
| `db_config.py` | Reads database configuration from environment variables. |
| `db_setup.py` | Creates the required `students` table. |
| `student_crud.py` | Demonstrates Create, Read, Update, and Delete operations. |
| `menu_app.py` | Menu-driven terminal application for student record management. |

## Database Table

The project creates a `students` table with the following fields:

| Column | Description |
| --- | --- |
| `student_id` | Auto-generated primary key. |
| `name` | Student name. |
| `email` | Unique student email. |
| `course` | Course name. |
| `marks` | Marks between 0 and 100. |
| `created_at` | Timestamp generated by PostgreSQL. |

## Setup

1. Install PostgreSQL locally.
2. Create a database named `tutedude_assignment_7`.
3. Create a `.env` file from `.env.example`.
4. Fill in your local PostgreSQL password in `.env`.
5. Install dependencies:

```powershell
pip install -r requirements.txt
```

## Run

Create the database table:

```powershell
python .\db_setup.py
```

Run the CRUD demonstration:

```powershell
python .\student_crud.py
```

Run the interactive app:

```powershell
python .\menu_app.py
```

## Evidence Requirements

- Full code must be visible in screenshots.
- Output must be visible in screenshots.
- System timestamp must be visible.
- Screenshots must be complete and unedited.

Suggested screenshots:

1. PostgreSQL database creation / pgAdmin or `psql` database list.
2. `.env.example`, `db_config.py`, and `db_setup.py` visible in editor.
3. Successful `python .\db_setup.py` terminal output.
4. `student_crud.py` visible in editor.
5. Successful `python .\student_crud.py` terminal output.
6. `menu_app.py` visible in editor.
7. Successful add/view/update/delete flow in `python .\menu_app.py`.

## Submission Checklist

- [x] Add dependency file.
- [x] Add environment variable template.
- [x] Implement PostgreSQL connection configuration.
- [x] Implement database table setup.
- [x] Implement CRUD practical script.
- [x] Implement menu-driven student management application.
- [ ] Verify database setup and connection details locally.
- [ ] Capture required screenshots.
- [ ] Organize screenshots in lecture/practical order.
- [ ] Create ZIP or PDF evidence package as allowed by the assignment.
- [ ] Confirm the submitted package opens correctly.
