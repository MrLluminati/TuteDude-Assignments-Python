# Assignment 3: Module 4 - Functions & Modules in Python

## What This Assignment Teaches

Reusable functions and importing Python's standard `math` module.

## Decisions and Rationale

- Planned one script for the factorial function and one script for math module calculations to match the two PDF tasks.
- The factorial task should expose a named `factorial` function because the PDF specifically asks for it.
- The math task should import `math` and use `sqrt`, `log`, and `sin` directly so the standard library usage is visible.
- Input validation should be minimal but useful, especially because square root and natural log have domain limits.

## Current Work Log

- [x] Source PDF captured.
- [x] Folder and README scaffolded.
- [x] Compatible module note captured from the ZIP resource pack.
- [x] Task 1 implemented with a named `factorial` function and loop-based multiplication.
- [x] Task 2 implemented with `math.sqrt`, `math.log`, and `math.sin`.
- [x] Local validation completed with factorial, math, invalid input, and syntax checks.

## Next Learning Step

Package this folder as a ZIP when it is time to submit Assignment 3.
